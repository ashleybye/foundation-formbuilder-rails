<%= form_for @continent, builder: Foundation::FormBuilder::Rails::FormBuilder do |f| %>

  <%= @continent.errors.full_messages if @continent.errors.any? %>

  <%= f.zurb_time_zone_select "name", nil, include_blank: true %>



  <%= f.submit %>

<% end %>