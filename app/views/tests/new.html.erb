<%= form_for @continent, builder: Zurb::FoundationFormBuilder do |f| %>

  <%= @continent.errors.full_messages if @continent.errors.any? %>

  <%= f.zurb_select :id, Continent.all.collect { |c| [c.name, c.id] },
    {include_blank: true, label: {label:"Test"}} %>



  <%= f.submit %>

<% end %>