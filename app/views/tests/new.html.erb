<%= form_for @city, builder: Zurb::FoundationFormBuilder do |f| %>

  <%= @city.errors.full_messages if @city.errors.any? %>

  <%= f.zurb_grouped_collection_select :country_id, @continents, :countries, :name, :id, :name %>



  <%= f.submit %>

<% end %>